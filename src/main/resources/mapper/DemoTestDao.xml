<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.ling.other.mapper.DemoTestDao">

    <sql id="baseSql">
       bean_name, method_name, method_params, cron_expression, remark, job_status
    </sql>


    <insert id="addSysJob">
        insert into au_task(bean_name, method_name, method_params, cron_expression, remark, job_status)
        values(
            #{beanName},
            #{methodName},
            #{methodParams},
            #{cronExpression},
            #{remark},
            #{jobStatus}
        )
    </insert>

    <select id="getSysJobListByStatus" resultType="com.ling.other.common.schedule.config.SysJobPO">
        select
            job_id,
            <include refid="baseSql"></include>
        from au_task
        where job_status = #{status}
    </select>
</mapper>