package com.ling.other.modules.scheduler.utils;

import com.ling.other.modules.scheduler.dto.JobProgress;

/**
 * @author zhangling
 * @since 2020/9/14 14:46
 */
public class SchedulerTool {

    /**
     * 获取当前任务进度
     */
    //public JobProgress getJobProgress() {
        //JobProgress progress = this.getProgressCache().getCache(logId, redisHelper);
        //if (progress == null) {
        //    progress = new JobProgress().setProgress(0).setMessage("");
        //}
        //return progress;
    //}
}
